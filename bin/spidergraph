#!/usr/bin/env babel-node --

var program = require('commander');

program
  .version("0.0.1")
  .command("scrape <url>", "Extracts data from a given URL.")
  .command("crawl <url>", "Extracts URLs and navigates list.")
  .parse(process.argv);
